{{-- @foreach ($data as $item)
    {{ $item->id }}
@endforeach --}}
{{-- {{ $data }} --}}
{{-- INSERT INTO hansdb.userCompatibilities (user_data_id,daily_quota) VALUES
@for ($i = 0; $i < count($data); $i++)
    ({{ $data[$i]->id }},-2),<br>
@endfor --}}
